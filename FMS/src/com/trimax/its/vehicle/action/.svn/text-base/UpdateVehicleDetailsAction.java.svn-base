package com.trimax.its.vehicle.action;


import java.text.SimpleDateFormat;
import java.util.Date;

import org.apache.struts2.ServletActionContext;

import com.trimax.its.vehicle.dao.UpdateVehicleDetailsDAO;
import com.trimax.its.vehicle.model.BodyType;
import com.trimax.its.vehicle.model.BrandType;
import com.trimax.its.vehicle.model.MakeType;
import com.trimax.its.vehicle.model.ModelType;
import com.trimax.its.vehicle.model.OrganisationChart;
import com.trimax.its.vehicle.model.Vehicle;
import com.trimax.its.vehicle.model.VehicleType;

public class UpdateVehicleDetailsAction {

	/*



	public void setBodyTypeName(String bodyTypeName) {
		this.bodyTypeName = bodyTypeName;
	}


	public String saveEditVehicleDetails() {

		UpdateVehicleDetailsAction values = new UpdateVehicleDetailsAction();
		Vehicle vehicleObject = new Vehicle(); 
		
		OrganisationChart orgChart = new OrganisationChart();
		orgChart.setOrg_chart_id(depotOrUnitId);
		
		BrandType brandType = new BrandType();
		brandType.setBrand_type_id(brandId);
		
		ModelType modelType= new ModelType();
		modelType.setModel_type_id(modelTypeId);
		
		MakeType makeType = new MakeType();
		makeType.setMake_type_id(makeTypeId);
		
		VehicleType vehicleType = new VehicleType();
		vehicleType.setVehicle_type_id(vehicleTypeId);

		BodyType bodyType = new BodyType();
		bodyType.setBody_type_id(bodyTypeId);
		bodyType.setBody_type_name(bodyTypeName);
		vehicleObject.setVehicleId(vehicleId);
		vehicleObject.setRegistrationDate(registrationDate);
		vehicleObject.setVehicleRegistrationNumber(registrationNumber);
		vehicleObject.setDepotOrUnit(orgChart);
		vehicleObject.setMakeType(makeType);
		vehicleObject.setModelType(modelType);
		vehicleObject.setBrandType(brandType);
		vehicleObject.setBodyType(bodyType);
		vehicleObject.setVehicleType(vehicleType);
		
		vehicleObject.setFcExpiryDate(fcExpiryDate);
		vehicleObject.setProgressingKMs(progressingKms);
		vehicleObject.setAcAvailability(acAvaialbility);
		vehicleObject.setProceduredDate(proceduredDate);
		vehicleObject.setOperationalDate(operationalDate);
		vehicleObject.setVehicleUsageCategory(vehicleUsage);
		vehicleObject.setWheelsCount(wheelsCount);
		vehicleObject.setChasisNumber(chasisNumber);
		vehicleObject.setStatus(status);
		vehicleObject.setKmpl(kmpl);
		vehicleObject.setDockingPlanningDate(dockingDate);
		vehicleObject.setUnderWarranty(underWarranty);
		vehicleObject.setUpdated_date((new SimpleDateFormat("yyyy-MM-dd hh:mm:ss")).format(new Date()));
		vehicleObject.setDeleted_status(0);
		
		UpdateVehicleDetailsDAO daoobject = new UpdateVehicleDetailsDAO();
		daoobject.saveEditedVehicleDetails(vehicleObject);
		ServletActionContext.getRequest().getSession().setAttribute("vehilceID",vehicleId);
		VehicleAction vehicleAction = new VehicleAction();
		vehicleAction.getParticularVehicleDetails();
		
		return "success";
	}*/

}
